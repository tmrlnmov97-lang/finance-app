/* Подключение к Supabase.
   url     — Project URL из Settings → API
   anonKey — публичный ключ anon / publishable оттуда же.
   Ключ не секрет: к строке пускают только политики RLS её владельца. */
window.FINANCE_CONFIG = {
  url: '',
  anonKey: ''
};
